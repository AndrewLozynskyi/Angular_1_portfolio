/*
 * @author ohmed
 * Dashboard targets management
*/

var Targets = {};

Targets.create = function ( companyId, title, params, callback ) {

    // todo

};

Targets.remove = function ( companyId, filterId, callback ) {

    // todo

};

Targets.update = function ( companyId, filterId, title, params, callback ) {

    // todo

};

Targets.getList = function ( companyId, callback ) {

    // todo

};

//

module.exports = Targets;
